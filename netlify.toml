[build]
  base    = "client"
  command = "npm install && npm run build"
  publish = "client/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes serve index.html so React Router works on refresh
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
